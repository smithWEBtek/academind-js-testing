import { it, expect } from 'vitest'
import { showError } from './dom'

it('should update the DOM with the given error message', () => {
  // const errorMsg = 'Something is wrong!'
  // showError(errorMsg);

  // const result = document.getElementById('errors')
  // expect(result.textContent).toEqual(errorMsg);
})
